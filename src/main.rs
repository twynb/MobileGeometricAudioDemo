#![allow(dead_code)]
// remove this once integrating - this is to avoid exessive and useless warnings for the time being

mod scene;
mod interpolation;
mod ray;
mod chunk;
mod scene_bounds;

fn main() {
    println!("Hello, world!");
}
